<%= form_for(@tipodoc) do |f| %>
  <% if @tipodoc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipodoc.errors.count, "error") %> prohibited this tipodoc from being saved:</h2>

      <ul>
      <% @tipodoc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar"%>
  </div>
<% end %>
