<%= form_for(@tiponovedad) do |f| %>
  <% if @tiponovedad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tiponovedad.errors.count, "error") %> prohibited this tiponovedad from being saved:</h2>

      <ul>
      <% @tiponovedad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
