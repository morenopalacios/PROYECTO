<%= form_for(@centro) do |f| %>
  <% if @centro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@centro.errors.count, "error") %> prohibited this centro from being saved:</h2>

      <ul>
      <% @centro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
